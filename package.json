{
  "name": "colony-portal-network",
  "version": "1.0.0",
  "description": "",
  "main": "truffle-config.js",
  "scripts": {
    "preinstall": "git submodule update --remote --recursive && cd lib/colonyNetwork && yarn",
    "deploy-contracts": "cd lib/colonyNetwork && ./node_modules/.bin/truffle migrate --compile-all --reset"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "chai": "^4.1.2",
    "truffle-assertions": "^0.2.0"
  }
}
